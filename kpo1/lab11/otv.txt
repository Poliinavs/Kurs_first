count- возвращает количество раз, которое 3 входит в заданный диапазон( target1-значение для сопоставления)
[]-захват параметров, (список парметров если приходя аргументы в функцию)  {тело}
int num_items3 = count_if(v.begin(), v.end(), [](int i) { return i % 3 == 0; })
count if - возвращает количество элементов удовлетвор условию [](int i) - пустные скобки значит ничего не зазватывает 

for (auto i : v) [i]() {if (i % 3 == 0) cout << i << " "; }();
переменной i по очередеи присваиваются значения v, в функуию передается локальная копия i, захваченная по значению, auto - автоматический вывод типа